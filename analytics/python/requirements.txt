# Core dependencies for the quant_engine package
cvxpy>=1.3
psycopg[binary]>=3.1

# Optional dependency providing gradient-based optimization support
# Comment out if PyTorch is not desired in your environment.
torch>=2.1
