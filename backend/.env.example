NODE_ENV=development
# Change this if another service already occupies port 4000 on your host.
PORT=4000
# When running the backend directly on your machine (outside Docker),
# set this to "localhost". Keep the default "postgres" when the app
# runs inside Docker Compose.
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=algobank
POSTGRES_PASSWORD=algobank
POSTGRES_DB=algobank
REDIS_URL=redis://redis:6379
# Toggle to true to publish alerts to Redis Streams.
ENABLE_REDIS_STREAMS=false
# Comma or space separated list of origins allowed to access the API.
# Include every dev server that talks to this backend.
CORS_ALLOWED_ORIGINS=http://localhost:5173 http://localhost:8080
JWT_ACCESS_SECRET=change-me-access
JWT_REFRESH_SECRET=change-me-refresh
JWT_ACCESS_TTL=15 minutes
JWT_REFRESH_TTL=7 days
# Base64 encoded PEM values
RSA_PRIVATE_KEY=
RSA_PUBLIC_KEY=
# Hashicorp Vault Transit configuration (optional)
VAULT_ADDR=
VAULT_TOKEN=
VAULT_TRANSIT_PATH=transit/algobank
VAULT_ENCRYPTION_KEY=algobank-transit
